/*
1. Calculate the factorial of 5 using reduse method.
2. Find The Maximum of n numbers passed from function using reduse.
3. Pass more than one string argument from function and check whether the name prajjal is
   appear how many times.
*/

//1.

      // const factorial = (...nums)=>{
      //    return nums.reduce((fact, num)=>fact*num, 1);
      // };

      // let number=5;
      // console.log(factorial(1,2,3,4,5));


//2.

      const maximum = (...nums)=>{
         nums.reduce((max, num)=>max>num){
         }

      };
      console.log(factorial(1,2,3,4,5));
